"use strict";

/**
 * dsh-desktop — preload script (M1: intentionally empty).
 *
 * Kept as a stable seam for later milestones (exposing app version, tray IPC,
 * window controls to the renderer, etc.). The DSH web UI talks to its own
 * backend over HTTP, so no Node bridge is needed yet.
 */
